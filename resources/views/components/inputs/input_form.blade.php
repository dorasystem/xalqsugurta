<div class="col-md-{{ $col }} d-flex flex-column justify-content-end">
    <label for="{{ $name }}" class="form-label">{{ __($label) }}</label>
    <input type="text" id="{{ $name }}" placeholder="{{ __($placeholder) }}" name="{{ $name }}" class="form-input">
</div>
